language: java

sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer

before_install:
  - chmod +x gradlew
  - chmod +x release.sh

stages:
  - name: build
  - name: deploy
  - name: release
    if: branch = release # only release when building the "release" branch

jobs:
  include:

    # run gradle build
    - stage: build
      language: java
      script: ./gradlew build

    # publish snapshot to oss.jfrog.org
    - stage: deploy
      language: java
      script: ./gradlew artifactoryPublish -x test -Dsnapshot=true -Dbintray.user=$BINTRAY_USER -Dbintray.key=$BINTRAY_KEY -Dbuild.number=$TRAVIS_BUILD_NUMBER

    # release a new version to bintray
    - stage: release
      script: ./gradlew bintrayPublish -x test -Dbintray.user=$BINTRAY_USER -Dbintray.key=$BINTRAY_KEY -Dbuild.number=$TRAVIS_BUILD_NUMBER
